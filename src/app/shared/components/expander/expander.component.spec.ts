import { ExpanderComponent } from './expander.component';

describe('ExpanderComponent', () => {
  let component: ExpanderComponent;

  beforeEach(() => {
    component = new ExpanderComponent();
  })

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
